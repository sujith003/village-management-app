venv/
env/
__pycache__/
*.pyc
*.pyo
*.egg-info/

# Local SQLite (the project uses MySQL; this is only a stray local file)
db.sqlite3

# Environment variables (keep .env.example committed, exclude the rest)
.env
.env.local

# Uploaded media (user-generated content, not part of the codebase)
media/

# Collected static files (generated by `collectstatic` at deploy time)
staticfiles/
