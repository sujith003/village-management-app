# Copy this file to .env (or .env.production for a production build)
# and set it to your deployed backend URL, no trailing slash.
# If unset, the app falls back to http://127.0.0.1:8000 for local dev.
VITE_API_BASE_URL=http://127.0.0.1:8000
